ALU test: Test ALU ops like addition and the like. 
But only test ALU operations

aluops.py: generates program.iram, the instruction ram values
that we run

outport.verify.dat: the output port values, in the relevant order

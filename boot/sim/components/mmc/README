Virtual MMC card

This is a relatively simple virtual-MMC card which operates on the 
"rising-edge latches, falling edge shifts" principle. 

The MMC card, on each read, outputs 512 bytes counting from 0-255 twice


Test the NetControl eproc

--------------------------------------------------
TEST THE COUNTER INTERFACE
--------------------------------------------------

We query and reset the counter using a device at address CNTDEVICE
(CNTDEVICEint). 

1. query counters
2. increment, query, and reset counters

--------------------------------------------------
TEST THE NICSERIAL INTERFACE
--------------------------------------------------

FIXME : we still need to do this. 


--------------------------------------------------
TEST THE MEMDEBUG INTERFACE
--------------------------------------------------
1. can we read RAMDQ delays? 
2. can we set and read the MEMDDR2 interface? 
3. 
